[build]
  command = "cd frontend && npm install && npm run build"
  publish = "frontend/dist"

[context.production.environment]
  VITE_API_URL = "https://mbg-production.up.railway.app" # Placeholder URL
